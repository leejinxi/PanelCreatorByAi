"""Integration tests exercise the same confirmed contract as the demo."""
from tools.cad_tools import DEFAULT_MCP_CONTRACT_PATH


def confirmed_boundary_contract(test):
    return DEFAULT_MCP_CONTRACT_PATH
