# 本地 MCP 回环 PoC 阶段性报告

日期：2026-09-09
状态：阶段 1–3 已完成；阶段 4 尚未开始

## 目标

在无法取得公司 CAD API 资料的条件下，使用个人 PC 自拟契约验证 Python Agent 与 MCP 的本地 STDIO 通信原理。此 PoC 不连接真实 CAD，不证明实验字段与公司 API 一致。

## 已完成阶段

### 阶段 1：实验契约

- 文件：`contracts/FULL_contract_with_data.json`
- 版本：`0.1-poc`
- 状态：`confirmed`
- 仅定义 `create_panel`。
- 输入字段：`referenceName`、`thicknessMm`、`material`、`boundaries`。
- Mock 场景：默认成功、`REFERENCE_PLANE_NOT_FOUND`、`CAD_UNAVAILABLE`。
- 已加强空白字符串拒绝及成功/失败输出互斥。

### 阶段 2：Mock MCP STDIO Server

- `mcp_mock/contract.py`：读取已确认契约并校验输入/输出 Schema 和样例。
- `mcp_mock/server.py`：注册 Tool、校验参数、匹配数据并返回 `structuredContent`。
- 已使用真实 MCP SDK 验证 `initialize`、`tools/list` 和 `tools/call`。

### 阶段 3：MCP Client 与 CAD Backend

- `mcp_client/stdio_client.py`：以当前 Python 启动独立 STDIO Server 子进程。
- `tools/mcp_cad_backend.py`：实现现有 `CadBackend` 接口。
- 字段映射：`reference_plane -> referenceName`、`thickness -> thicknessMm`。
- MCP 业务失败经 `CreatePanelTool` 转换为 `CadExecutionResult`。
- 已覆盖 `MCP_TIMEOUT`、`MCP_UNAVAILABLE`、`MCP_INVALID_RESPONSE` 和业务错误码。

## 当前未完成

当前默认路径仍是：

```text
LangGraph -> create_panel() -> MockCadBackend
```

目标路径尚未接通：

```text
浏览器 -> FastAPI -> LangGraph -> Ollama
  -> create_panel() -> McpCadBackend
  -> MCP STDIO Server -> FULL_contract_with_data.json
  -> Web 页面
```

具体缺口：

- `tools/cad_tools.py` 尚无 `CAD_BACKEND=mcp` 选择逻辑。
- Web DTO 尚无 `mcp` 执行模式。
- `/api/health` 尚不会报告 `mcp-contract-mock`。
- 页面尚不会显示 MCP Contract Mock 标识。
- 尚无固定 LLM 输出的 Web/Graph/MCP 综合测试。
- 尚未运行真实 Ollama + Web + MCP 的人工全系统测试。

## 当前测试证据

环境：Python 3.11.15，conda `ai_cad_agent`。
命令：`python -X utf8 run_tests.py`。
结果：94 项运行，93 项通过，1 项真实 Ollama E2E 默认跳过。

关键测试：

- `tests/test_local_contract.py`
- `tests/test_mock_mcp_server.py`
- `tests/test_mcp_cad_backend.py`

## 换 PC 后继续步骤

1. 阅读 `AGENTS.md`、`Doc/current_status.md`、本报告和 `Doc/TODO.md`。
2. 执行：

```powershell
conda activate ai_cad_agent
python --version
python -m pip install -r requirements.txt
python -X utf8 run_tests.py
ollama list
```

3. 检查 `qwen2.5:7b` 是否存在。
4. 实现配置化 Backend 选择，默认值必须为 `mock`。
5. 增加 `mcp` Web 模式和健康状态。
6. 添加固定 LLM 输出的全链路自动测试。
7. 启动真实 Ollama 和 Web，完成五类人工场景。
8. 通过后更新 README，并明确记录“真实 MCP 通信已验证，CAD 行为仍为 Contract Mock”。

## 推荐环境变量

```powershell
$env:CAD_BACKEND = "mcp"
$env:MCP_CONTRACT_PATH = "contracts/FULL_contract_with_data.json"
$env:MCP_TIMEOUT_SECONDS = "10"
python run_web.py
```

这些变量在阶段 4 完成前尚不会全部生效。

## 工作区提醒

截至本报告生成时，本批契约、MCP 源码、测试和文档仍可能是未提交修改。切换 PC 前必须检查 `git status --short`，通过获准的 Git/文件同步方式带走全部文件；不要只复制已提交历史。
